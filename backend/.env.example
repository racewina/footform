# API-Football v3 (https://www.api-football.com) — direct, NOT via RapidAPI.
APIFOOTBALL_KEY=your_api_sports_key_here
# Defaults are fine; override only if needed.
# APIFOOTBALL_HOST=v3.football.api-sports.io
# APIFOOTBALL_MIN_GAP_MS=150   # spacing between upstream calls; raise for low-tier plans

# Server
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:5173
